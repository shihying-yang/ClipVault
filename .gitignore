# 打包 .crx 用的私鑰，不進版控（manifest 裡的 key 是公鑰，公開沒問題）
key.pem
.DS_Store
.claude/

# 打包檔不進版控
*.zip

# 本地 OAuth／API 憑證：真正的值只存在這裡，不進 git 歷史
.env

# 由 tools/build_manifest.py 從 manifest.template.json + .env 產生，
# 每個人本地跑出來的內容不同，不該進版控
extension/manifest.json
