# 複製這份成 .env（.env 已經被 .gitignore 排除，不會進版控）

# Google OAuth client_id：類型要選「網頁應用程式」，redirect URI 要登記
#   https://EXTENSION_ID.chromiumapp.org/
# 執行 tools/build_manifest.py 時會印出目前的 EXTENSION_ID，也可以自己用
# extension/manifest.template.json 裡固定的 key 算（README 有算法）。
#
# 這次用的是完整 https://www.googleapis.com/auth/drive scope（不是
# drive.file）——因為要支援「指定既有路徑、逐層搜尋/自動建立資料夾」，
# 這個操作需要查詢既有檔案結構，drive.file scope 不允許。Google 會把
# 這個 scope 標成「敏感」，自己測試用的話不受影響，只是授權畫面會多一句
# 「這個應用程式未經 Google 驗證」，點「進階」繺續即可；要正式上架給別人
# 用才需要走 Google 的審查流程。
GOOGLE_OAUTH_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
