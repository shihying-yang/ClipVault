# 複製這份成 .env（.env 已經被 .gitignore 排除，不會進版控）

# Google OAuth client_id：類型要選「網頁應用程式」，redirect URI 要登記
#   https://EXTENSION_ID.chromiumapp.org/
# 執行 tools/build_manifest.py 時會印出目前的 EXTENSION_ID，也可以自己用
# extension/manifest.template.json 裡固定的 key 算（README 有算法）。
#
# scope 用的是 drive.file（非敏感、免審查），不需要跟 Google 要額外驗證。
GOOGLE_OAUTH_CLIENT_ID=your-client-id-here.apps.googleusercontent.com

# 同一個 OAuth 用戶端頁面上就會顯示 client secret，直接複製貼上。
# Drive 授權走 Authorization Code 流程（換 refresh_token），這樣 access
# token 過期後可以在背景直接續期，不用每次都重新跳出瀏覽器視窗、也不
# 會有「每天都要重新同意」的狀況。「網頁應用程式」類型的 client 換 token
# 這一步 Google 會要求帶 client secret；這個值不算真正機密（原理跟
# client_id 一樣，公開型應用程式本來就沒有真正保密的手段），只是抽到
# .env 裡讓它不進 git 歷史，純粹整潔考量。
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret-here
